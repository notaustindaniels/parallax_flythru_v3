// @vectorflight/renderer-svg — (world, tS, opts) -> SVG string (node) | mounted updater (browser).
// P2 lands renderFrameSVG (static frame); P3 adds composition.html + the Seekable
// Composition Contract window.vf (SPEC §6.2, §6.4). Determinism lint bans apply here.
export {};
