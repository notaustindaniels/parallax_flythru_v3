// @vectorflight/harness — `vf verify`: physics invariants on exported frames (SPEC §5.1, §8.3).
// Pure TS, no native deps. The block-matching optical-flow core was validated by spike S3
// (spikes/s3-block-flow — see docs/decisions.md); its matcher moves here, productionized, at P3/P6.
export {};
