# SPEC §2: pnpm pre/post scripts disabled; §7 supply chain.
# Dependency build scripts are denied by default; the explicit allowlist lives in
# package.json -> pnpm.onlyBuiltDependencies (ffmpeg-static needs its binary fetch).
enable-pre-post-scripts=false
engine-strict=true
